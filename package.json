{
  "name": "releaseforge",
  "version": "0.1.0",
  "description": "Issue-driven release readiness tooling for OSS CLIs and apps.",
  "type": "module",
  "bin": {
    "releaseforge": "./bin/releaseforge.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test tests/*.test.mjs",
    "lint": "tsc -p tsconfig.json --noEmit",
    "prepack": "npm run build",
    "smoke": "npm run build && node bin/releaseforge.js --help && node bin/releaseforge.js --version",
    "package:smoke": "npm run build && node scripts/package-smoke.mjs .",
    "release:check": "npm run lint && npm test && npm run smoke && npm run package:smoke && npm pack --dry-run"
  },
  "keywords": [
    "release",
    "github-actions",
    "oss",
    "cli",
    "continuous-release"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "files": [
    "bin/",
    "dist/",
    "templates/",
    "README.md",
    "LICENSE"
  ],
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^5.9.3"
  }
}
